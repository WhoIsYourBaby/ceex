import 'package:flutter/material.dart';

class KlineItem extends StatelessWidget {
  const KlineItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}